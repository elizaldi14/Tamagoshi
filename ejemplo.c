#include <stdio.h>
#include <unistd.h>

int main(){
/*
int contador = 0;

  for (int i = 0; i < 10; i++) {
    contador++;
    printf("contador = %d\n", contador);
    sleep(10);
  }
*/

/*Obtenemos el nombre del tamagoshi*/
char name[20];
printf("Hola papu que papu nombre le quieres poner a un tachillomi: ");
scanf("%s", name);



return 0;

}